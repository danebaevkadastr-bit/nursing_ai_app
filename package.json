{
  "name": "nursing_ai_app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node server_gemini_live.js",
    "start:old": "cd backend && node server.js"
  }
}
